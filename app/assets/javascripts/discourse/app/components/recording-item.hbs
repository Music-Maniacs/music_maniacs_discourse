<li class="track-item">
  <div class="album-art">
    <img src={{this.recording.cover_art_url}} alt="{{this.recording.name}} album art">
  </div>
  <div class="track-info">
    <div class="track-title">
      {{this.recording.name}}
      <span class="track-duration">{{format-track-length this.recording.length}}</span>
    </div>
    <div class="track-artist">{{this.recording.artist_name}}</div>
  </div>
  <div class="track-controls">
    <DButton
      class="icon-btn favorite {{if this.isFavorite 'active'}}"
      @icon="heart"
      @action={{@onFavorite}}
      @translatedTitle="Add to favorites" />

    <DButton
      class="icon-btn add"
      @icon="plus"
      @action={{@onAdd}}
      @translatedTitle="Add to playlist" />

    <DButton
      class="icon-btn more"
      @icon="ellipsis-v"
      @action={{@onMore}}
      @translatedTitle="More options" />

    <DButton
      class="play-button"
      @icon="play"
      @action={{@onPlay}}
      @translatedTitle="Play track" />

    <div class="play-count">
      {{d-icon "play"}}
      <span>{{format-number 10 compact=true}}</span>
    </div>
  </div>
</li>